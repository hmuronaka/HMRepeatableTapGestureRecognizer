import XCTest
@testable import HMRepeatableTapGestureRecognizer

final class HMRepeatableTapGestureRecognizerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HMRepeatableTapGestureRecognizer().text, "Hello, World!")
    }
}
